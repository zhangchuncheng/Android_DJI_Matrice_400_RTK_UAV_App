# 第三方机场集成开发详设文档

## 1. 概述

### 1.1 项目背景

本项目旨在将第三方无人机机场（中研宏科机巢）接入大疆上云API平台，使Web端无需修改代码即可通过标准的Dock3协议控制第三方机场。

### 1.2 功能目标

| 功能模块 | 描述 |
|---------|------|
| 心跳消息解析 | 订阅第三方机场心跳，解析状态数据 |
| 属性映射转换 | 将第三方机场状态转换为Dock3格式上报 |
| 远程调试指令 | 支持上云API远程调试指令下发到第三方机场 |
| 响应消息转发 | 将第三方机场响应包装为上云API格式返回 |

### 1.3 协议文档

| 文档 | 说明 |
|------|------|
| 第三方机场MQ协议 | 中研宏科无人机机巢MQ协议V2.2 |
| 上云API Dock3属性 | https://developer.dji.com/doc/cloud-api-tutorial/cn/api-reference/dock-to-cloud/mqtt/dock/dock3/properties.html |
| 上云API Dock3远程调试 | https://developer.dji.com/doc/cloud-api-tutorial/cn/api-reference/dock-to-cloud/mqtt/dock/dock3/cmd.html |

---

## 2. 架构设计

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        Web平台                                 │
│  (发送上云API指令、接收Dock3格式状态和响应)                       │
└──────────────────────────┬──────────────────────────────────────┘
                           │ MQTT
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                    CloudApiService                              │
│  (接收上云API指令，分发到无人机或第三方机场服务)                   │
└──────────────────────────┬──────────────────────────────────────┘
                           │
          ┌────────────────┴────────────────┐
          ▼                                 ▼
┌─────────────────────┐      ┌─────────────────────────────┐
│   无人机控制模块     │      │   ThirdPartyAirportService │
│ (MSDK飞行控制)       │      │  (第三方机场协议转换)        │
└─────────────────────┘      └──────────────┬──────────────┘
                                            │ MQTT
                                            ▼
                               ┌─────────────────────────────┐
                               │      第三方机场(机巢)        │
                               │ (中研宏科无人机机巢)        │
                               └─────────────────────────────┘
```

### 2.2 核心组件

| 组件 | 职责 | 文件路径 |
|------|------|----------|
| ThirdPartyAirportService | 第三方机场协议处理核心类 | `mqtt/ThirdPartyAirportService.kt` |
| CloudApiService | 上云API服务，集成第三方机场服务 | `mqtt/CloudApiService.kt` |
| MqttManager | MQTT连接管理，支持第三方机场消息监听 | `mqtt/MqttManager.kt` |
| ConfigManager | 配置管理，包含第三方机场配置项 | `config/ConfigManager.kt` |

---

## 3. 数据映射设计

### 3.1 心跳消息映射

第三方机场心跳 → Dock3设备属性映射表：

| 第三方机场字段 | Dock3属性 | 类型转换说明 |
|--------------|-----------|-------------|
| `air_conditioner.air_conditioner_state` | `air_conditioner.air_conditioner_state` | 直接映射 |
| `air_conditioner.switch` | `air_conditioner.switch_time` | 映射为0 |
| `batteries[].capacity_percent` | `drone_battery_maintenance_info.batteries[].capacity_percent` | 字符串转int |
| `batteries[].index` | `drone_battery_maintenance_info.batteries[].index` | 直接映射 |
| `batteries[].voltage` | `drone_battery_maintenance_info.batteries[].voltage` | 直接映射 |
| `batteries[].temperature` | `drone_battery_maintenance_info.batteries[].temperature` | 转float |
| `drone_charge_state.state` | `drone_charge_state.state` | 直接映射 |
| `alarm_state` | `alarm_state` | 直接映射 |
| `activation_time` | `activation_time` | 直接映射 |
| `humidity` | `humidity` | 转float |
| `temperature` | `temperature` | 转float |
| `environment_temperature` | `environment_temperature` | 转float |
| `wind_speed` | `wind_speed` | 字符串转float |
| `rainfall` | `rainfall` | 直接映射 |
| `drone_in_dock` | `drone_in_dock` | 直接映射 |
| `supplement_light_state` | `supplement_light_state` | 直接映射 |
| `cover_state` | `cover_state` | 状态值映射 |
| `sub_device.device_online_status` | `sub_device.device_online_status` | 直接映射 |
| `mode_code` | `mode_code` | 直接映射 |
| `latitude` | `latitude` | 字符串转double |
| `longitude` | `longitude` | 字符串转double |
| `sn` | `sn` | 直接映射 |

### 3.2 舱盖状态映射

| 第三方机场状态 | 含义 | Dock3状态 |
|--------------|------|-----------|
| 0 | 关闭 | 0 |
| 1 | 打开 | 1 |
| 2 | 打开中 | 1 |
| 3 | 关闭中 | 0 |

---

## 4. 远程调试指令映射

### 4.1 指令映射表

| 上云API方法 | 第三方机场方法 | 参数说明 |
|------------|--------------|----------|
| `charge_close` | `charge_close` | 无参数 |
| `charge_open` | `charge_open` | 无参数 |
| `cover_close` | `cover_close` | 无参数 |
| `cover_open` | `cover_open` | 无参数 |
| `cover_force_close` | `cover_close` | 无参数 |
| `putter_close` | `putter_close` | 无参数 |
| `putter_open` | `putter_open` | 无参数 |
| `lift_down` | `lift_down` | 无参数 |
| `lift_up` | `lift_up` | 无参数 |
| `lift_back` | `lift_back` | 无参数 |
| `lift_turn` | `lift_turn` | 无参数 |
| `drone_close` | `drone_close` | 无参数 |
| `drone_open` | `drone_open` | 无参数 |
| `device_reboot` | `device_reboot` | 无参数 |
| `device_sys_reboot` | `device_sys_reboot` | 无参数 |
| `alarm_state_switch` | `alarm_state_switch` | `action`: 0-关闭, 1-开启 |
| `supplement_light_close` | `supplement_light_close` | 无参数 |
| `supplement_light_open` | `supplement_light_open` | 无参数 |
| `handle_close` | `handle_close` | 无参数 |
| `handle_open` | `handle_open` | 无参数 |
| `air_conditioner_close` | `air_conditioner_close` | 无参数 |
| `air_conditioner_open` | `air_conditioner_open` | 无参数 |
| `debug_mode_close` | `debug_mode_close` | 无参数 |
| `debug_mode_open` | `debug_mode_open` | 无参数 |
| `hangar_log_minio_upload` | `hangar_log_minio_upload` | 无参数 |

### 4.2 请求消息格式

**上云API格式（Web端下发）：**
```json
{
    "tid": "uuid-tid",
    "bid": "uuid-bid",
    "method": "cover_open",
    "params": {},
    "timestamp": 1745393335703
}
```

**第三方机场格式（App转发）：**
```json
{
    "tid": "uuid-tid",
    "bid": "uuid-bid",
    "method": "cover_open",
    "gateway": "airport-sn",
    "data": {},
    "timestamp": 1745393335703
}
```

### 4.3 响应消息格式

**第三方机场响应：**
```json
{
    "tid": "uuid-tid",
    "bid": "uuid-bid",
    "method": "cover_open",
    "gateway": "airport-sn",
    "data": {
        "result": 0,
        "output": {
            "status": "ok"
        }
    },
    "timestamp": 1745393335703
}
```

**上云API格式响应（App转发）：**
```json
{
    "tid": "uuid-tid",
    "bid": "uuid-bid",
    "method": "cover_open",
    "data": {
        "result": 0,
        "output": {
            "status": "ok"
        }
    },
    "timestamp": 1745393335703
}
```

---

## 5. 核心类设计

### 5.1 ThirdPartyAirportService

**类职责：**
- 订阅第三方机场心跳消息
- 将心跳数据转换为Dock3格式上报
- 封装远程调试指令，下发给第三方机场
- 处理上云API指令到第三方机场指令的映射

**核心方法：**

| 方法 | 功能 | 参数 | 返回值 |
|------|------|------|--------|
| `handleHeartbeat()` | 处理心跳消息 | `topic`: String, `payload`: String | 无 |
| `convertAndReportAsDock3()` | 转换心跳为Dock3属性 | `heartbeat`: JsonObject | 无 |
| `handleServiceReply()` | 处理服务响应 | `topic`: String, `payload`: String | 无 |
| `sendServiceRequest()` | 发送服务请求 | `method`: String, `tid`: String, `bid`: String, `params`: Map | 无 |
| `handleDock3ServiceRequest()` | 处理上云API指令映射 | `method`: String, `tid`: String, `bid`: String, `params`: Map | Boolean |

**远程调试方法（18个）：**

| 方法名 | 功能说明 |
|--------|----------|
| `chargeClose()` | 关闭充电 |
| `chargeOpen()` | 打开充电 |
| `coverClose()` | 关闭舱盖 |
| `coverOpen()` | 打开舱盖 |
| `putterClose()` | 夹紧推杆 |
| `putterOpen()` | 松开推杆 |
| `liftDown()` | 下降升降台 |
| `liftUp()` | 上升升降台 |
| `liftBack()` | 回位旋转台 |
| `liftTurn()` | 旋转旋转台 |
| `droneClose()` | 飞行器关机 |
| `droneOpen()` | 飞行器开机 |
| `deviceReboot()` | 机场重启 |
| `deviceSysReboot()` | 机场系统重启 |
| `alarmStateSwitch()` | 机场声光警报开关 |
| `supplementLightClose()` | 关闭补光灯 |
| `supplementLightOpen()` | 打开补光灯 |
| `handleClose()` | 关闭手柄 |
| `handleOpen()` | 开启手柄 |
| `airConditionerClose()` | 关闭空调 |
| `airConditionerOpen()` | 开启空调 |
| `debugModeClose()` | 调试模式关闭 |
| `debugModeOpen()` | 调试模式开启 |
| `hangarLogMinioUpload()` | 上传日志到minio服务器 |

### 5.2 CloudApiService集成

**集成方式：**

1. 在`CloudApiService`中初始化`ThirdPartyAirportService`单例
2. 在`handleServiceRequest()`方法中，优先调用`thirdPartyAirportService.handleDock3ServiceRequest()`
3. 如果第三方机场服务处理成功（返回true），则直接返回
4. 如果处理失败（返回false），则继续处理无人机指令

```kotlin
// 先尝试由第三方机场服务处理远程调试指令
if (config.thirdPartyAirportEnabled && thirdPartyAirportService.handleDock3ServiceRequest(method, tid, bid, params)) {
    return
}
// 继续处理无人机指令...
```

### 5.3 MqttManager消息路由

**消息路由逻辑：**

| Topic匹配规则 | 处理组件 |
|--------------|----------|
| 包含`heartbeat` | ThirdPartyAirportService.onHeartbeatReceived() |
| 包含`services_reply` | ThirdPartyAirportService.onServiceReplyReceived() |
| 以`/services`结尾 | CloudApiService.handleServiceRequest() |
| 以`/property/set`结尾 | CloudApiService.handlePropertySet() |
| 以`/drc/down`结尾 | CloudApiService.handleDrcDown() |

---

## 6. 配置设计

### 6.1 配置项

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `thirdPartyAirportEnabled` | Boolean | false | 是否启用第三方机场 |
| `thirdPartyAirportHost` | String | "" | 第三方机场MQTT地址 |
| `thirdPartyAirportPort` | Int | 1883 | 第三方机场MQTT端口 |
| `thirdPartyAirportTopic` | String | "" | 第三方机场控制主题 |
| `thirdPartyAirportSn` | String | "" | 第三方机场SN码 |

### 6.2 配置页面

配置页面位于`ConfigFragment`，包含：
- 启用开关：控制是否启用第三方机场功能
- 机场地址：第三方机场MQTT服务器地址
- 机场端口：MQTT端口号
- 控制主题：第三方机场服务主题（如`thing/product/{sn}/services`）
- 机场SN码：第三方机场设备序列号

---

## 7. 消息流程

### 7.1 心跳上报流程

```
第三方机场 → MQTT心跳消息 → MqttManager → ThirdPartyAirportService
                                                   ↓
                                            解析心跳数据
                                                   ↓
                                            转换为Dock3属性
                                                   ↓
                                            上报到Web平台
                                       (thing/product/{sn}/osd)
```

### 7.2 远程调试指令流程

```
Web平台 → MQTT服务请求 → MqttManager → CloudApiService
                                               ↓
                                      解析method和params
                                               ↓
                                      调用ThirdPartyAirportService
                                               ↓
                                      转发到第三方机场
                                      (thing/product/{sn}/services)
                                               ↓
                                      接收第三方机场响应
                                      (thing/product/{sn}/services_reply)
                                               ↓
                                      包装为Dock3格式响应
                                               ↓
                                      返回给Web平台
                                      (thing/product/{sn}/services_reply)
```

---

## 8. 开发注意事项

### 8.1 数据类型转换

第三方机场返回的数据类型可能与Dock3不一致，需要注意：
- 字符串类型的数字需要转换为对应数值类型
- 嵌套结构需要逐层解析

### 8.2 状态值映射

部分状态值需要进行映射转换，如舱盖状态（见3.2节）

### 8.3 异常处理

- 心跳解析失败时记录日志，不中断服务
- 服务响应超时需要处理
- 网络断开时自动重连

### 8.4 线程安全

- MQTT消息回调在后台线程，UI更新需要切换到主线程
- 全局单例使用`@Volatile`确保线程安全

---

## 9. 文件清单

| 文件 | 说明 |
|------|------|
| `mqtt/ThirdPartyAirportService.kt` | 第三方机场服务核心类 |
| `mqtt/CloudApiService.kt` | 上云API服务，集成第三方机场 |
| `mqtt/MqttManager.kt` | MQTT连接管理，消息路由 |
| `config/AppConfig.kt` | 配置数据模型 |
| `config/ConfigManager.kt` | 配置管理 |
| `pages/ConfigFragment.kt` | 配置页面 |
| `res/layout/frag_config_page.xml` | 配置页面布局 |

---

## 10. 测试方案

### 10.1 心跳解析测试

1. 模拟第三方机场心跳消息
2. 验证Dock3属性是否正确转换
3. 验证OSD上报消息格式是否正确

### 10.2 远程调试指令测试

1. 发送上云API远程调试指令
2. 验证第三方机场是否收到对应指令
3. 验证响应消息是否正确返回给Web端

### 10.3 端到端测试

1. Web端发送`cover_open`指令
2. 验证第三方机场舱盖是否打开
3. 验证Web端是否收到成功响应
